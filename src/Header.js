import React from 'react';

const Header=function(){

    const headerStyle={textAlign:'center',backgroundColor:'black',color:'white',textTransform:'uppercase'};
    return(
        <div style={headerStyle}>
      Phone directory
    </div>
    )
    
}

export default Header;